<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.CategoryMapper">

    <select id="selectCategoryBackVO" resultType="com.blog.model.vo.CategoryBackVO">
        SELECT c.id, c.category_name,COUNT(a.id) AS article_count,c.create_time
        FROM category c
        LEFT JOIN article a
        ON c.id = a.category_id
        <where>
            <if test="keyword != null">
                name like concat('%',#{keyword},'%')
            </if>
        </where>
        GROUP BY c.id
        ORDER BY c.id DESC
        LIMIT #{limit},#{size}
    </select>

    <select id="selectCategoryVO" resultType="com.blog.model.vo.CategoryVO">
        SELECT c.id,
               c.category_name,
               COUNT(a.id) AS article_count
        FROM category c
        LEFT JOIN article a ON c.id = a.category_id
        GROUP BY c.id
    </select>

</mapper>